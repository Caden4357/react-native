import React, {useState} from 'react';
import { View, Text} from 'react-native';

const Camera = (props) => {
    return (
        <View>
            <Text>Camera</Text>
        </View>
)}

export default Camera;